import styled from "styled-components"

export const PriceListComponent = styled.div`
  padding: 60px 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 40px;

  .top {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
  }
`
